# Action Cable adapter. With AnyCable, WebSockets are served by anycable-go (not Puma).
# See config/anycable.yml and README.md.
development:
  adapter: any_cable

test:
  adapter: test

production:
  adapter: any_cable
