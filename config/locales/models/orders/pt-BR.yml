pt-BR:
  activerecord:
    attributes:
      order:
        discount: Desconto
        status: Status
        table: Mesa
    errors:
      models:
        order:
          attributes:
            status:
              invalid_transition: "não é possível alterar de %{from} para %{to}"
