en:
  activerecord:
    attributes:
      order:
        discount: Discount
        status: Status
        table: Table
    errors:
      models:
        order:
          attributes:
            status:
              invalid_transition: "cannot transition from %{from} to %{to}"
